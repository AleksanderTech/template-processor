{
  "name": "string-interpolation",
  "version": "1.0.0",
  "description": "Template processing using regex",
  "scripts": {
    "test": "mocha test/**/*.js"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^10.0.0"
  }
}
